import React from 'react'function ClockLabel({caption}) {  return <span>Time:</span>}export default class Clock extends React.Component {  state = {  // состояния    dateTime: new Date(),    format: "24"  }  componentDidMount = () => { // интервал обновлений таймера (жизненный цикл)    this.timer = setInterval(_ => {      this.setState({          dateTime: new Date(),        })    }, 1000)  }  componentWillUnmount = () => {    clearInterval(this.timer)  }  setFormat (event) { //устанавливаем значение состояния формата    this.setState({format: event.target.value})  }  render() {    let {dateTime, format} = this.state;    console.log(      dateTime.toLocaleString('en-US', { hour: 'numeric', minute: 'numeric', hour12: true })    )    return <div>      <ClockLabel />      {" "}      <span>        {(() => {          switch (this.state.format) { // перебираем состояния и в соответсвии с ним выводим таймер            case "24":   return <span>{dateTime.toLocaleTimeString()}</span>;            case "12": return <span>{dateTime.toLocaleString('en-US', { hour: 'numeric', minute: 'numeric',  second: 'numeric', hour12: true })}</span>;            default: return {format};          }        })()}      </span>      <div onChange={this.setFormat.bind(this)}>        <input type="radio" value="12" name="format" /> 12        <input type="radio" value="24" name="format"/> 24      </div>    </div>  }}